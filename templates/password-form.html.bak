{% if password %}
{% set required = "required" if request.args.get("page") == "register" else "" %}
<div class=separator></div>
<h3>{{ "Definir" if required else "Cambiar" }} Contraseña</h3>
<div class=item-fields>
	<div class=item-field>
	<label for=pass>Contraseña</label><input {{ required }} type=password name=pass />
	</div>
	<div class=item-field>
	<label for=pass_repeat>Repetir Contraseña</label><input {{ required }} type=password name=pass_repeat />
	</div>
	<div class=item-field>
		<label for=show_pass>Mostrar Contraseña</label>
		<input type=checkbox name=show_pass onclick="togglePasswords()"/>
	</div>
</div>
{% endif %}
